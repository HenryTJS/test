.content {
  padding: 20px;
  text-align: center;
}
.word-list {
  display: flex;
  flex-wrap: wrap; /* 自动换行 */
  justify-content: space-between; /* 每行组件均匀分布 */
  gap: 10px; /* 控制组件之间的间距 */
  padding: 10px;
  width: 100%;
  box-sizing: border-box;
}